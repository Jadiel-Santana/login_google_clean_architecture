name: login
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4
  flutter_modular:
  mobx:
  flutter_mobx:
  dartz:
  equatable:
  connectivity:
  json_annotation:

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito:
  mobx_codegen:
  modular_codegen:
  build_runner:
  json_serializable:
  effective_dart:

flutter:
  uses-material-design: true

  assets:
    - assets/images/